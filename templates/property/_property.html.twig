<div class="card mb-4">
    {#
        {% if property.filename %}
            <img src="{{ vich_uploader_asset(property, 'imageFile') | imagine_filter('thumb') }}" class="card-img-top" alt="img" style="width:100%; height:auto;">  
        {% else %}
            <img src="{{ '/images/properties/Rental_vector_2.jpg' | imagine_filter('thumb') }}" class="card-img-top" alt="img" style="width:100%; height:auto;">  
        {% endif %}
    #}

       
        {% set first = property.images|first %}
        {% if first %}
            <img src="{{ asset(uploaded_images_directory ~ first.name) | imagine_filter('thumb') }}" class="card-img-top w-100" alt="img" style="">  
        {% else %}
            <img src="{{ asset('/images/pngtree-cartoon-european-classical-house-image_1278656.jpg') | imagine_filter('thumb') }}" class="card-img-top" alt="img" style="width:100%; height:auto;">  
        {% endif %}
   

    <div class="card-body">
        <h5 class="card-title">
            <a href="{{ path('property.show', {slug: property.slug, id: property.id}) }}">
                {{ property.title }}
            </a>
        </h5>
        <p class="card-text"> {{ property.surface }} m² - {{ property.city }} ({{ property.postalCode }}) </p>
        <div class="text-primary" style="font-size: 2rem; font-weight: bold";>
            {{ property.formattedPrice }} €
        </div>
    </div>
</div>