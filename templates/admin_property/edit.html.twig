{% extends 'base.html.twig' %}

{% block title %}Editer le bien{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Editer le bien</h1>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
        
        {{ include('admin_property/_form.html.twig', {'button_label': 'Enregistrer'}) }}

        
        <div class="mt-4">
            <a href="{{ path('app_admin_property_index') }}" class="me-2">Revenir à la liste</a>

            {{ include('admin_property/_delete_form.html.twig') }}
        </div>

    </div>
{% endblock %}

